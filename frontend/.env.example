# Copy to frontend/.env.local if the API is not on the default origin.
#
# It must match the backend's GOOGLE_CALLBACK_URL host and be listed as
# WEB_ORIGIN on the backend side — credentialed CORS allows exactly one
# origin (§D20).
VITE_API_URL="http://localhost:3000"
