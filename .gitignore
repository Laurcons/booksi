# credentials — never commit
local/
.env
.env.*
!.env.example

# dependencies & build output
node_modules/
dist/
build/
*.tsbuildinfo

# end-to-end run artefacts (traces and screenshots of failures)
test-results/
playwright-report/

# raw device captures submitted through /probe/report — headers and manual
# visual-judgment answers from whichever Kobo tested it, not project source
kobo-frontend/reports/

# logs
npm-debug.log*
*.log

# editors / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
