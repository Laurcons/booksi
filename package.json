{
  "name": "bookcsi",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "shared",
    "backend",
    "frontend",
    "kobo-frontend"
  ],
  "scripts": {
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "build:shared": "npm run build --workspace shared",
    "dev:api": "npm run build:shared && npm run start:dev --workspace backend",
    "dev:web": "npm run dev --workspace frontend",
    "dev:kobo": "npm run build:shared && npm run dev --workspace kobo-frontend",
    "build": "npm run build:shared && npm run build --workspace backend && npm run build --workspace frontend && npm run build --workspace kobo-frontend",
    "test": "npm run test --workspace backend && npm run build:shared && npm run test --workspace frontend && npm run test --workspace kobo-frontend",
    "test:api": "npm run test --workspace backend",
    "test:web": "npm run build:shared && npm run test --workspace frontend",
    "test:kobo": "npm run build:shared && npm run test --workspace kobo-frontend",
    "test:e2e": "npm run build:shared && npm run test:e2e --workspace frontend && npm run test:e2e --workspace kobo-frontend",
    "typecheck": "npm run build:shared && npm run typecheck --workspace backend && npm run typecheck --workspace frontend && npm run typecheck --workspace kobo-frontend",
    "lint": "npm run lint --workspace frontend && npm run lint --workspace kobo-frontend"
  },
  "allowScripts": {
    "prisma@6.19.3": true,
    "@prisma/client@6.19.3": true,
    "@prisma/engines@6.19.3": true
  }
}
